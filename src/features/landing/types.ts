export type NavItem = {
  label: string
  href: string
}

export type TextCard = {
  title: string
  text: string
  icon: string
}

export type StepItem = {
  title: string
  text: string
  active?: boolean
}

export type Testimonial = {
  name: string
  role: string
  text: string
}

export type PainPoint = {
  label: string
  icon: string
}
